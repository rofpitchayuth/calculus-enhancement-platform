# KT Microservice — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework & ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Data validation (Pydantic v2 is bundled with FastAPI >= 0.100)
pydantic>=2.0.0

# Deep learning inference (CPU wheel is sufficient; replace with torch+cuda if GPU is available)
torch>=2.0.0

# Numerical operations used in the one-hot encoding step
numpy>=1.24.0

# HTTP client — used by the backend integration service (kt_service.py)
# to call this microservice from the main backend
httpx>=0.27.0
