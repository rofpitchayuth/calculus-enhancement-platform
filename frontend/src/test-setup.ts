/**
 * test-setup.ts — Global test bootstrap
 * ======================================
 * Registers jest-dom matchers (e.g. toBeInTheDocument) for all test files
 * and stubs browser APIs that jsdom does not provide.
 */
import "@testing-library/jest-dom";
